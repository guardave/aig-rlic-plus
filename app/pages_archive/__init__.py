# Package marker for archived portal pages.
#
# Streamlit auto-discovers pages under `app/pages/`. Pages that have been
# archived (no longer user-facing) are moved here to prevent auto-listing
# on the landing page while preserving the code + git history.
#
# Do NOT import modules from this package in production code.
# Do NOT add new pages here — new pages go in `app/pages/`.
