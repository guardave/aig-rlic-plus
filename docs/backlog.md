# AIG-RLIC+ Rule Backlog

**Owner:** Lead Lesandro
**Authority:** `META-BL` (Backlog Discipline) — see `docs/agent-sops/team-coordination.md` §META-BL
**Purpose:** Registry of proposed rules that were considered, discussed, and **deliberately deferred** — preserving the proposal, its motivation, and the reactivation trigger so future Leads (or the same Lead at a later session) can pick up the thread without re-deriving it from scratch.

---

## How to use this file

- **When a rule is proposed but not adopted:** add a row here with the proposer, the proposed rule ID (if one was drafted), the motivation, the decision (deferred / rejected), the reason for deferral, the reactivation trigger, and the date.
- **At EOD:** Lead scans the `reactivation_trigger` column; any item whose trigger has fired is either re-opened (promoted to SOP) or explicitly closed (trigger fired but decision unchanged — add a dated note to the row).
- **When an item is promoted:** strike through the row, add a pointer to the SOP section where it now lives, and keep the row for historical traceability.
- **When an item is rejected outright:** move to the "Rejected Proposals" section at the bottom with a one-line rationale.

Backlog entries are NOT active rules. They have no enforcement authority until promoted to an SOP and registered in `docs/standards.md`.

---

## Active Deferrals

| ID | proposer_agent | proposed_rule_id | motivation | decision | deferred_reason | reactivation_trigger | date |
|----|----------------|------------------|------------|----------|-----------------|----------------------|------|
| BL-001 | Ace | APP-SEV1-MAP | Validation severity (L1/L2/L3) currently assigned per-call by Ace; proposal is to move to a lookup JSON so severity is consistent across components | Deferred to next sprint per Lesandro 2026-04-19 | Current ad-hoc assignments work; mechanization doesn't affect what stakeholders see; other Wave 5B rules have higher stakeholder-visible ROI | Triggered if (a) inconsistent severity causes a real bug, OR (b) after Pair #10 when severity drift becomes more likely with scale | 2026-04-19 |
| BL-002 | Quincy | (ECON-UD cross-pair retro-apply) | Wave 7 introduced `signal_scope.json` for HY-IG v2; the 5 other completed pairs (indpro_spy, sofr_ted_spy, dff_ted_spy, ted_spliced_spy, permit_spy, vix_vix3m_spy, hy_ig_spy sample) lack this file. ECON-UD's Universe Disclosure requirement is not uniformly met across the portal. | Deferred per Lesandro 2026-04-20 | Wave 7 cross-pair audit confirmed 0 active scope leaks on other pairs (they were clean by construction — only HY-IG v2 had the leak). Universe Disclosure gap exists but no stakeholder-visible symptom; retro-apply is hygiene, not urgent. | Triggered when (a) next pair is promoted to reference-pair candidacy, OR (b) stakeholder asks to see the Universe on a non-HY-IG-v2 pair, OR (c) during Pair #4 execution when fresh SOP compliance is the baseline | 2026-04-20 |
| BL-003 | Quincy | (ECON-AS cross-pair placeholder) | Companion to BL-002: the 5 non-HY-IG-v2 completed pairs lack `analyst_suggestions.json` files. ECON-AS is informational only; placeholder files (even if empty) would make the schema contract visible on every pair's Methodology page | Deferred per Lesandro 2026-04-20 | Lower priority than BL-002 — if no suggestions exist, an empty file is low-value until agents naturally file entries during future analysis work | Triggered alongside BL-002 (bundle the work) | 2026-04-20 |
| BL-004 | Ace + Quincy | (APP-NP1 Narrative-to-Page Prose Sourcing) | Wave 7C-1 QA caught that portal `.py` pages contain hardcoded `st.markdown()` prose that bypasses Ray's narrative markdown authority — Ray cleaned CCC-BB from the narrative but Ace's page files had their own prose saying the same wrong thing. This is a structural gap: narrative authority is split across `docs/portal_narrative_*.md` AND `app/pages/*.py` with no contract for sourcing. Proposed rule would require page prose to source from narrative frontmatter/anchors, limiting inline page code to chart captions + structural labels. | Deferred per Lesandro 2026-04-20 | Not urgent — Ace's Wave 7C fix-up closed the HY-IG v2 instance. But the pattern will recur: Sample HY-IG pages likely have the same gap, and every new pair starts with Ace writing some inline prose. A proper rule would change the component architecture. | Triggered when (a) another narrative-vs-page drift incident surfaces, OR (b) Pair #4 execution starts (force Ace to write pages with the new discipline from scratch rather than retrofit), OR (c) a scheduled refactor wave dedicated to the `app/components/narrative_renderer` abstraction | 2026-04-20 |
| BL-801 | Quincy | (APP-WS2 winner_summary key discipline) | Wave 8C QA caught: `app/pages/9_hy_ig_v2_spy_strategy.py:197` reads `_winner.get("max_drawdown", -0.102)` but the schema field is `oos_max_drawdown`. The `.get()` ALWAYS falls through to fallback because the key is wrong. Display currently shows -10.2% only because fallback happens to equal reality. This is the exact META-UC latent class. Fix: either (a) rename producer field to match consumer OR (b) update consumer to use schema-correct key, AND add a producer-side keys-in-use check during validate_or_die. | Deferred per Lesandro 2026-04-20 | Stakeholder-visible values happen to be correct by coincidence; fix is narrow (1 line change + validator extension). Bundling with other consumer unit-coherence work makes sense; doing alone now would split Wave 8 unnecessarily. | Triggered when (a) winner_summary.json schema changes max_drawdown's default/type, OR (b) next consumer-audit wave, OR (c) Pair #4 start where Ace writes pages with the strict schema-key discipline | 2026-04-20 |
| BL-802 | Quincy | (ECON-TO Turnover Basis Declaration) | Wave 8C QA-CL2 triangulation surfaced a 2.8× mismatch: HY-IG v2 shows annual_turnover=3.78/yr while implied turnover from n_trades/years/2 = 10.57/yr. Not a display bug — a definition mismatch. P2 Signal Strength's turnover uses position-change-weighted turnover (sum of \|Δposition\|), whereas implied turnover from trade counts assumes round-trip trades. Both are valid conventions; neither is documented in the schema. Proposal: add `turnover_basis: "round_trip" \| "position_change_weighted" \| "notional_ratio"` enum to winner_summary.schema.json; augment QA-CL2 to dispatch on basis. | Deferred per Lesandro 2026-04-20 | Not a bug — consistent with the strategy's P2 construction. But surfaces a schema gap: different strategy families report different turnover definitions silently. Fix is schema enum bump + producer validation + QA-CL2 dispatch logic. | Triggered when (a) a new pair uses a different strategy family and the convention clash becomes visible OR (b) during a scheduled schema evolution wave | 2026-04-20 |
| BL-VIZ-O1-LEGACY | Quincy (Wave 10H.1) | (VIZ-O1 retro-apply to legacy pairs) | Wave 10H.1 Quincy audit: 35 chart JSONs across 6 legacy pair directories (`dff_ted_spy`, `indpro_spy`, `permit_spy`, `sofr_ted_spy`, `ted_spliced_spy`, `vix_vix3m_spy`) have no matching `_meta.json` sidecar. VIZ-O1 (Disposition Mandate) requires every chart to carry a disposition. These legacy charts predate the rule and were not covered by Vera's backfill (backfill scanned only sidecars that already exist). Proposal: Vera dispatch to create minimal sidecars for each legacy chart with `"disposition": "consumed"` + baseline fields. Alternative: mark legacy pairs as "frozen" / exempted from VIZ-O1 and document in SOP. | Deferred pending Lead decision | Wave 10H.1 already consumed full Ace+Vera+Quincy dispatch budget; retro-apply on 35 legacy charts is hygiene, not shipping-critical. Active focus pairs (hy_ig_v2_spy, hy_ig_spy, indpro_xlp, umcsent_xlv) are 65/65 compliant. | Triggered when (a) any legacy pair is promoted to active rotation, OR (b) Wave 10H.2/10I scheduled hygiene wave, OR (c) GATE-28 scope extends to include legacy pairs in cloud verify | 2026-04-22 |
| BL-META-CMP | Lead (fix260526 retro) | META-CMP — Completeness Forcing Functions (Tier 1+2 first) | Three classes of completeness drift kept appearing in `fix260526` triage and the prior `gold_copper_xli` Mode-2 build: producer-vs-template (filename convention), producer-vs-schema (JSON contract), and text-vs-data (narrative claims contradicting source CSV; **3 confirmed instances in fix260526 alone**). All currently caught only by manual cloud render or by ad-hoc producer-side `jsonschema` gates I bolted onto individual pipelines. Proposal: bundle into a 4-tier forcing-function wave. **Tier 1** (pre-commit Git hook running `validate_all_schemas.py` + `smoke_loader.py --all` + `lint_filename_convention.py`) and **Tier 2** (chart-set completeness manifest lint against `pair_config.py`) ship together as the first META-CMP wave. **Tier 3** (text-vs-data citation lint) is the most valuable but is the biggest semantic change — separate follow-up. **Tier 4** (cloud-render gate in CI) Phase-2 deferred. Full proposal + acceptance criteria + cross-references in GH issue #7. | Deferred to new branch after `fix260526` merges | This branch's scope was the user-flagged Excel-log issues, not project-level SOP work. META-CMP is a structural change touching pre-commit infrastructure and would be premature to bundle with per-pair fixes. | Triggered when (a) the next text-vs-data drift incident surfaces (likely on the next pair build), OR (b) Lead opens a dedicated SOP-infrastructure branch, OR (c) a stakeholder reports a "page silently broke" issue that one of the four tiers would have caught. Tracked in GH issue #7. | 2026-05-27 |
| BL-VIZ-SIDECAR-HELPER | Vera (Wave 10H.1) | (Shared chart sidecar writer) | Wave 10H.1 Vera found that only 3 of 7 per-pair generators have a sidecar-writer function to patch for VIZ-O1 emission; the other 4 (`indpro_spy`, `indpro_xlp`, `ted_variants`, `umcsent_xlv`) use a bare `fig.write_json()` with sidecars originating elsewhere. Proposal: consolidate into `scripts/_chart_sidecar.py` shared helper, then update all generators to call it. Reduces drift surface. | Deferred per Vera's own flag | Not blocking: Wave 10H.1 focus pairs that matter have disposition field via backfill; future generator runs on the 4 flagged pairs would produce sidecar-less outputs without this refactor. | Triggered when (a) any of the 4 flagged pairs are regenerated, OR (b) Wave 10H.2/10I hygiene wave bundled with BL-VIZ-O1-LEGACY | 2026-04-22 |
| ~~BL-PERM-SUBAGENT~~ **CLOSED 2026-04-22** | Lead (Wave 10H.1) | (Subagent permission propagation diagnostic) | ~~Wave 10H.1 dispatched 3 subagents (Ace, Vera, Quincy) — all 3 hit sandbox denial on `Write(~/.claude/agents/<role>-<name>/memories.md)` despite project settings allowing it.~~ **Resolved:** Quincy's 3rd-attempt dispatch (commit `44a487a`) confirmed that both `~/.claude/agents/qa-quincy/memories.md` and `experience.md` writes SUCCEEDED via Edit tool after the single-slash → double-slash fix in `b3facc8`. The docs-derived diagnosis held: single-slash absolute paths were being interpreted as project-relative per Claude Code permissions.md. Root cause: settings file syntax, not subagent inheritance. Ace's Wave 10H.1 follow-up dispatch (commit `387062f`) confirmed fix a second time: both global-profile writes succeeded without prompt. | Resolved — fix `b3facc8` shipped and validated twice (Quincy + Ace). | N/A (closed) | 2026-04-22 |
| ~~BL-APP-PR1~~ **PROMOTED 2026-04-23** | Ace (Wave 10H.1) | APP-PR1 Path Resolution Discipline — **now live** in `docs/agent-sops/appdev-agent-sop.md` | Wave 10H.1 follow-up exposed that `_render_exploratory_insights` was correctly using `_REPO_ROOT` — but the broader class risk (a relative-path read silently failing because cloud CWD differs from repo root) was not codified. Proposal: mandate `_REPO_ROOT = Path(__file__).resolve().parents[N]` anchors for ALL project-relative reads in `app/components/**` and `app/pages/**`; prohibit bare relative strings like `Path("results")/…` or `open("results/…")`. Couple with an APP-SEV1 extension: path-exists-but-unreadable / JSON-parse failure on shipped-pair files must surface per L2 (`st.warning`) — silent-skip only permitted for path-does-not-exist AND optional-artifact. | Proposed by Ace (via handoff); deferred to a follow-up SOP hygiene wave | Ace's current audit of `page_templates.py` found 0 bare-relative instances (all 5 data reads go through `_REPO_ROOT`). Rule is prophylactic — no active bug; fix is SOP authoring + a grep-check CI lint. Bundles well with APP-PT1 legacy-page migration (BL-APP-PT1-LEGACY). | Triggered when (a) the next cloud vs local path-drift bug surfaces, OR (b) next SOP hygiene wave, OR (c) when APP-PT1 legacy page migration completes and the 5 bypass pages move to the template | 2026-04-23 |
| BL-APP-PT1-UMCSENT | Ray (Wave 10H.2) | (umcsent_xlv Strategy page APP-PT1 migration) | During Wave 10H.2 Ray narrative authoring, discovered `app/pages/10_umcsent_xlv_strategy.py` is a hand-rolled page that bypasses `render_strategy_page()`. Originally assumed on template; actually on legacy path. `umcsent_xlv_config.py` was never created — creating it now would produce orphan code that no page consumes. Net effect: APP-TL1's rich trade-log block will NOT reach umcsent_xlv until its Strategy page is migrated to the template (and a pair config created with `TRADE_LOG_EXAMPLE_MD`). **Subset of `BL-APP-PT1-LEGACY` with narrower scope** — isolate if umcsent_xlv migration is wanted ahead of the broader legacy wave. | Deferred | Non-blocking: user's trade-log regression ask is satisfied on the two template-based pairs that had the old view (hy_ig_spy, indpro_xlp). umcsent_xlv retains its legacy page, which is its pre-Wave-10H.2 state. | Triggered when (a) umcsent_xlv needs any APP-* rule addition that the template provides, OR (b) full `BL-APP-PT1-LEGACY` wave is scheduled | 2026-04-23 |
| BL-DATA-DICT-APPTL1 | Evan (Wave 10H.2) | (Per-pair data dictionaries covering broker-style CSV columns) | Wave 10H.2 Evan dispatch surfaced: `data/data_dictionary_indpro_xlp_*.csv` and `data/data_dictionary_umcsent_xlv_*.csv` do not exist. APP-TL1 introduces the broker-style CSV as a user-facing artifact with a 10-column canonical schema; Dana's data dictionaries should cover these columns for each pair that ships the artifact. Proposal: bundle with the hygiene wave — Dana adds per-pair dictionary files mirroring APP-TL1 columns. | Proposed by Evan (via handoff); deferred | Non-blocking — broker-style CSV schema is already documented in APP-TL1 itself and inherited from Sample's reference implementation. Dictionary files are user-facing documentation, not a runtime dependency. | Triggered when (a) stakeholder requests per-pair schema docs, OR (b) scheduled hygiene wave bundling with BL-APP-PT1-LEGACY + BL-VIZ-O1-LEGACY | 2026-04-23 |
| BL-COMMISSION-BASIS | Evan (Wave 10H.2) | (commission_bps field on winner_summary.json) | Wave 10H.2 Evan dispatch: neither `indpro_xlp` nor `umcsent_xlv` `winner_summary.json` carries a `commission_bps` field. Evan defaulted to 5 bps (matches Sample/HY-IG convention) for the broker-style CSV. If the original tournament used a different cost tier this is a silent lie at the display layer — class BL-801 of issue. Proposal: (a) add `commission_bps` to `winner_summary.schema.json` as required; (b) Evan audits the two pipelines to confirm 5 bps was the tournament basis (or rerun with the real basis); (c) broker-style CSV regenerates if the basis differs. | Proposed by Evan (via handoff); diagnostic pending | Non-blocking cosmetically — the default is likely right, but unverified. Best-in-wave fix is a 5-minute pipeline-script audit by Evan; escalates to tournament rerun only if basis mismatch is found. | Triggered at next pipeline touch on either pair, OR at hygiene wave with APP-TL1 retro-audit | 2026-04-23 |
| BL-VIZ-CHART-PREFIX-LEGACY | Ace (Wave 10I.A) | (Legacy chart filename drift — pair-id-prefixed files on 3 pairs) | Ace migration discovered `indpro_spy`, `permit_spy`, `vix_vix3m_spy` keep pair-id-prefixed chart filenames on disk (e.g. `output/charts/indpro_spy/plotly/indpro_spy_hero.json`) vs the VIZ-NM1 canonical bare-name convention (`hero.json`). Configs set `HERO_CHART_NAME` etc. to the prefixed values to keep smoke green. Proposal: Vera rename all legacy-pair chart files to canonical bare-names + update config overrides to defaults. Bundles with BL-VIZ-O1-LEGACY since both are chart-hygiene on the same pair set. | Deferred per Wave 10I.A scope | Non-blocking — smoke green, cloud works, just non-canonical filenames. Risk: config must carry explicit overrides indefinitely; any developer assuming bare-name convention will be surprised. | Triggered with BL-VIZ-O1-LEGACY hygiene wave | 2026-04-23 |
| BL-LEGACY-WINNER-SUMMARY-SHAPE | Evan (Wave 10I.A) | (6 legacy pairs' winner_summary.json broadly shape-drifted vs current schema) | Evan's Wave 10I.A schema relaxation (commit `2fa6c95`) surfaced that `threshold_value: null` is only the visible tip. 6 legacy pairs (`indpro_spy`, `permit_spy`, `vix_vix3m_spy`, `sofr_ted_spy`, `dff_ted_spy`, `ted_spliced_spy`) are missing 9+ required fields: `generated_at`, `signal_column`, `target_symbol`, `threshold_rule`, `strategy_family`, `oos_max_drawdown`, `oos_n_trades`, `oos_period_start`, `oos_period_end`. Also carry legacy extras tolerated only because `additionalProperties` is unset: `signal_display_name`, `threshold_code`, `lead_value`, etc. Plus `win_rate` is null in 7 of 11 pairs (not in schema today). Only passes today because `validate_or_die` runs on specific Strategy-page code paths rather than uniformly; strict validation would break all 6 pairs on many fields. Proposal: Evan + Dana backfill/regenerate the 6 legacy winner_summary.json to v1.1 shape; decide on `additionalProperties: false` + declare legacy-extra fields; add `win_rate` to schema (null-tolerant). Supersedes the narrower `BL-THRESHOLD-VALUE-SCHEMA` entry. | Deferred | Non-blocking — schema tolerance at `2fa6c95` + Ace's defensive coerce at `5f2e50d` keep cloud green. But any future uniform-validation rule will fail on these 6 pairs hard. Bundles with the hygiene wave that touches the same pair set. | Triggered when (a) strict-validation rule is added, OR (b) any of the 6 pairs gets tournament refresh, OR (c) scheduled hygiene wave bundling with BL-VIZ-O1-LEGACY et al. | 2026-04-23 |
| BL-THRESHOLD-VALUE-SCHEMA | Ace (Wave 10I.A) | (`threshold_value: null` on 6 legacy pairs' winner_summary.json) | Wave 10I.A cloud verify found 6 legacy pairs (`indpro_spy`, `permit_spy`, `vix_vix3m_spy`, `sofr_ted_spy`, `dff_ted_spy`, `ted_spliced_spy`) have `"threshold_value": null` in `winner_summary.json`. The active pairs (`hy_ig_v2_spy` 0.5, `hy_ig_spy` 0.5, `indpro_xlp` 0.75, `umcsent_xlv` 0.0) are all numeric. Ace shipped a defensive coerce in `instructional_trigger_cards.py` at `5f2e50d` that catches `TypeError`/`ValueError` and shows an APP-SEV1 L2 banner explaining the fallback. That unblocks cloud verify but the underlying producer-side schema gap remains. Proposal: (a) Evan backfills numeric `threshold_value` in the 6 legacy pairs by reading each tournament's winning-threshold code and materialising the real value; (b) Dana/Evan update `winner_summary.schema.json` to `{"type": "number"}` non-nullable; (c) pipeline guard rejects null at write time. | Deferred (consumer now defensive) | Non-blocking — users see a disclosed fallback banner, not a crash. Cosmetic / schema-cleanliness fix. Bundles naturally with BL-BROKER-CSV-LEGACY (overlapping 3 pairs) and BL-CHART-GAPS-LEGACY (overlapping 5 pairs) in a hygiene wave that touches the same pair set. | Triggered with the legacy-pair hygiene wave, OR when any of the 6 pairs is tournament-refreshed | 2026-04-23 |
| BL-BROKER-CSV-LEGACY | Ray (Wave 10I.A) | (Canonical APP-TL1 broker-style CSV missing on 3 legacy pairs) | Ray Wave 10I.A Part 3a discovered that `indpro_spy`, `permit_spy`, `vix_vix3m_spy` do NOT have the canonical APP-TL1 `winner_trades_broker_style.csv` artifact. They still carry only a legacy 5-column `winner_trade_log.csv` from their pre-APP-TL1 tournaments. Each pair's `TRADE_LOG_EXAMPLE_MD` explicitly flags the gap to the reader (Ray-authored disclosure); APP-TL1 template path runs but the broker-download button shows APP-SEV1 L2 info banner ("primary artifact missing — showing position log only"). Proposal: Evan dispatch to back-generate the 3 broker CSVs using the existing `scripts/_trade_log_broker.py::synthesize_from_position_log` helper (from commit `2c11046`), same treatment as `indpro_xlp`/`umcsent_xlv` got. | Deferred | Non-blocking — users see disclosed gap, not broken pages. APP-SEV1 L2 banners are the designed fallback for this state. | Triggered with BL-CHART-GAPS-LEGACY (same 3 of the 5 pairs) hygiene wave, OR when any of the 3 pairs gets a tournament refresh | 2026-04-23 |
| BL-CHART-GAPS-LEGACY | Ace (Wave 10I.A) | (Missing chart artifacts on 5 legacy pairs) | Ace migration discovered `permit_spy`, `vix_vix3m_spy`, `sofr_ted_spy`, `dff_ted_spy`, `ted_spliced_spy` lack `equity_curves`, `drawdown`, `walk_forward` chart files entirely. Pre-existing data gap — the original hand-written legacy pages (and TED composite) didn't render them either. Configs omit those `*_CHART_NAME` attrs; template falls back to placeholder at runtime. Proposal: Evan/Vera back-generate the missing charts so template rendering is complete. Or: codify the omission in the pairs' configs as intentional. | Deferred | Non-blocking — users never saw these charts pre-migration so no regression; new APP-PT1 path is at parity. But pairs are visibly less complete than template-compliant peers. | Triggered with hygiene-wave or when either pair needs a tournament refresh | 2026-04-23 |
| BL-APP-PT1-LEGACY | Ace (Wave 10H.1) | (Methodology page template migration for 5 legacy hand-written pages) | Wave 10H.1 Bug-2 root cause: `app/pages/9_hy_ig_v2_spy_methodology.py` is a hand-written legacy page that bypasses `render_methodology_page()`. The APP-PT2 Exploratory Insights helper was wired into the template but never reached this page until Ace added a direct call. **5 other Methodology pages also bypass the template** (per Ace's audit). Only `14_indpro_xlp` and `15_hy_ig_spy` use APP-PT1. Any future Methodology-page rule risks the same class of silent-regression — legacy pages absorb the rule iff Ace manually replicates it in each. Proposal: schedule an APP-PT1 migration wave that converts all 7 Methodology pages to thin template wrappers, eliminating the legacy layer. Estimated scope: 5 pages × (config extraction + thin wrapper + smoke) ≈ one agent-wave. | Deferred per Wave 10H.1 closure scope | Not urgent: Ace's direct-call fix in `387062f` closed the Sample Methodology gap; future Methodology-scope rules can be applied defensively until migration completes. But drift risk accrues with every new APP-* rule that touches the Methodology page. | Triggered when (a) another APP-* rule addition causes a legacy-page regression, OR (b) scheduled SOP hygiene wave bundling with BL-APP-PR1, OR (c) any legacy pair (HY-IG v2 Sample, indpro_spy, permit_spy, vix_vix3m_spy, ted variants, umcsent_xlv) needs substantial Methodology-page change | 2026-04-23 |
| BL-APP-NUM1 | Ace (fix260531) | (APP-NUM1 — Numeric Format Single Source) | fix260531 comment #63 root cause: `indpro_spy_config.py` had **+7.6%** in the KPI card (auto-formatted via `_format_ratio_pct(0.0765)`, banker's rounding) and **+7.7%** in three body-prose strings (hand-typed at a different rounding). Same source value (`oos_ann_return = 0.0765`), two different rendered strings. The fix260531 tactical fix aligned indpro_spy by hand, but the bug class is structural: any prose author can re-introduce hand-typed percent/scalar strings in `*_config.py` that drift from the template's auto-formatted KPI. Proposal: SOP rule that (a) all performance-metric scalars in prose MUST go through the same formatter as the KPI card (helper-injected at render time), OR (b) `*_config.py` must not contain hand-typed `\d+\.\d+%` strings for the performance-set fields, with a producer-side lint that greps for this pattern in `app/pair_configs/*.py`. Tier-2 forcing-function candidate (bundle with BL-META-CMP). Retro-apply: cross-pair audit of `*_config.py` for hand-typed performance percentages, fix any drift. | Deferred to SOP-hardening branch | Tactical fix on indpro_spy closes the user-visible #63 instance; the SOP work needs deliberate design (helper injection vs lint vs both) and is wider than the current branch's scope. Hybrid path agreed with Lesandro 2026-05-31. | Triggered when (a) the next stakeholder comment surfaces another rounding-drift instance, OR (b) the META-CMP SOP-hardening branch opens, OR (c) cross-pair audit detects ≥2 other pairs with the same pattern | 2026-05-31 |
| BL-VIZ-NS1 | Vera + Ace (fix260531) | (VIZ-NS1 — Indicator Naming Standard) | fix260531 comment #64 root cause: indpro_spy used ≥4 different references to the same indicator on the Story page alone — `Industrial Production`, `INDPRO`, `IP YoY Growth`, `IP Growth Quartile`, plus chart-data labels `IP accel`/`IP contraction`/`IP mom_3m`/etc. No naming style guide existed for indicator references across titles/axes/legends/prose. The tactical fix standardised indpro_spy on `INDPRO` as the canonical short form with one long-form first-mention `Industrial Production (INDPRO)`. Proposal: SOP rule that each pair declares `display_names` in `signal_scope.json` (or a sibling `display_names.yaml`) with `short_name`, `long_name`, `axis_labels[]`, and that all chart titles/axes/legends + Story prose use the canonical short form, except for a single long-form first mention per page. Vera dispatch + lint to enforce. Retro-apply: audit all 10 active pairs for naming-drift; fix any found. | Deferred to SOP-hardening branch | Same rationale as BL-APP-NUM1 — tactical fix closes indpro_spy; the SOP needs to be designed against `signal_scope.json` schema and applied across pairs deliberately. | Triggered when (a) the next stakeholder comment surfaces a naming-inconsistency instance on another pair, OR (b) META-CMP SOP-hardening branch opens, OR (c) `signal_scope.json` schema bump for a different reason | 2026-05-31 |
| BL-VIZ-LO1 | Vera (fix260531) | (VIZ-LO1 — Legend / Caption Vertical Separation) | fix260531 user-reported "general issue, just an example" — on the `indpro_spy` Dot-Com Crash history-zoom chart the horizontal legend overlaps the bottom source-note caption. Cross-pair audit found **60 charts across all 10 active pairs** with the same overlap class: (a) 38 `history_zoom_*.json` charts where `temp/generate_history_zoom_charts.py` emitted `legend.y=-0.05`, `caption.y=-0.12`, `margin.b=60` — only 7px of paper space between two text rows; (b) 22 `rolling_correlation` + `rolling_granger` charts where `scripts/viz_cp_retro_apply.py` emitted `legend.y=-0.35`, `caption.y=-0.22`, `margin.b=80` — legend rendered inside the bottom margin. Tactical fix in this branch: `scripts/patch_legend_caption_layout.py` idempotently rewrites all 60 chart JSONs to non-overlapping values (history-zoom: legend.y=-0.18, caption.y=-0.32, margin.b=120; rolling: legend.y=-0.50, caption.y=-0.22, margin.b=140). Generator sources also patched: `scripts/viz_cp_retro_apply.py` updated; `temp/generate_history_zoom_charts.py` promoted to `scripts/generate_history_zoom_charts.py` with corrected constants so the institutional knowledge stops being untracked. **SOP proposal**: a VIZ-LO1 rule for chart-layout discipline — (i) when both a legend and a caption annotation sit below the plot area, the vertical separation between their `y` values MUST be ≥ 0.10 paper units; (ii) `margin.b` MUST be ≥ `|min(legend.y, caption.y) * height| + 20px` for the lowest element to render inside the canvas; (iii) producer-side smoke check `scripts/lint_chart_layouts.py` to enforce. Retro-apply: covered by this branch (patcher is idempotent and covers all currently-shipped charts); the SOP encodes the rule so a future generator can't reintroduce. | Deferred to SOP-hardening branch | Tactical fix closes the user-visible instance and the cross-pair class; the SOP-as-lint piece (smoke check, generator-side enforcement) needs design and bundles with the other three SOP proposals from this branch. | Triggered when (a) META-CMP SOP-hardening branch opens, OR (b) any new chart generator is added and reintroduces overlap, OR (c) Vera dispatches a chart-layout audit wave bundling VIZ-NS1/VIZ-DC1/VIZ-LO1 | 2026-05-31 |
| BL-VIZ-DC1 | Vera (fix260531) | (VIZ-DC1 — Bidirectional Chart Colour Discipline) | fix260531 comment #68 root cause: `scripts/generate_charts_indpro_spy.py:chart_granger` colored both `INDPRO->SPY` and `SPY->INDPRO` series red because the test `"INDPRO" in direction` is True for both strings (substring search). Fix was: split on `->` and check `cause == "INDPRO"`. **Class of bug**: any bidirectional chart (Granger, transfer entropy, lead/lag) using substring search on direction labels will exhibit the same colour-collapse. Plus: the chart had no "which direction is leading" annotation — viewer could see two lines but not interpret which one being below p=0.05 means what. Proposal: SOP rule that (a) bidirectional charts MUST identify direction by splitting on the canonical separator (`->` or `→`), not substring search; (b) MUST use distinct colours per direction (one indicator-colour, one target-colour); (c) MUST carry an in-chart annotation or caption that explains the colour key AND how "leading" is read from the chart (e.g. "line below p=0.05 = that direction leads"). Retro-apply: cross-pair audit of all bidirectional charts. Estimate 1-2 other pairs likely affected (CCF on any pair that has a SPY-leads-X reverse-direction view). | Deferred to SOP-hardening branch | Tactical fix closes the indpro_spy Granger chart; cross-pair audit is the structural work. Bundles naturally with VIZ-NS1 (both are Vera-owned chart-hygiene SOPs). | Triggered when (a) the next bidirectional-chart drift surfaces, OR (b) META-CMP SOP-hardening branch opens, OR (c) cross-pair audit finds ≥1 other affected chart | 2026-05-31 |

---

## Rejected Proposals

*(none yet)*

---

*Last updated: 2026-05-31 (Lead Lesandro, fix260531 tactical-fix branch; +4 entries: BL-APP-NUM1, BL-VIZ-NS1, BL-VIZ-DC1, BL-VIZ-LO1 SOP-hardening candidates)*
